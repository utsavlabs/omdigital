# .env.example
# This file serves as a template for environment variables.
# For Vercel deployments, add these variables in the Vercel Dashboard Settings under "Environment Variables".
# Never commit the actual `.env` file containing real secrets.

# Example variable (currently unused since the site is fully static):
# FUTURE_API_KEY=your_secret_key_here
